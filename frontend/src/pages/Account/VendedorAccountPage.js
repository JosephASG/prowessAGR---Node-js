import React from 'react';

export const VendedorAccountPage = ({ userRole, setRole, setIsLoggedIn }) => {
  // Aquí va el contenido específico para vendedores
  return (
    <div>
      <h1>Cuenta del Vendedor</h1>
      <p>Bienvenido, este es el panel de control para vendedores.</p>
      {/* Agrega aquí el contenido específico para la cuenta del vendedor */}
    </div>
  );
};

export default VendedorAccountPage;
